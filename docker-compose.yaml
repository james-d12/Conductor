name: conductor
services:
  api:
    build:
      context: .
      dockerfile: src/Conductor.Api/Dockerfile
    ports:
      - "8080:8080"
    develop:
      watch:
        - action: sync+restart
          path: .src/Conductor.Api
          target: .src/Conductor.Api
  cli:
    build:
      context: .
      dockerfile: src/Conductor.Cli/Dockerfile
    develop:
      watch:
        - action: sync+restart
          path: .src/Conductor.Cli
          target: .src/Conductor.Cli